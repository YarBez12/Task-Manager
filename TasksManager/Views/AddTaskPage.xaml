<?xml version="1.0" encoding="utf-8"?>

<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:controls="clr-namespace:TasksManager.Views.Controls"
             xmlns:vm = "clr-namespace:TasksManager.ViewModels"
             x:Class="TasksManager.Views.AddTaskPage">
    <ContentPage.BindingContext>
        <vm:TaskViewModel></vm:TaskViewModel>
    </ContentPage.BindingContext>
    <VerticalStackLayout>
        <Label Text="Add New Task" FontSize="24" HorizontalOptions="Center" />
        <controls:TaskControl />
        <!-- <Button Text="Save Task" Command="{Binding SaveTaskCommand}" /> -->
    </VerticalStackLayout>
</ContentPage>